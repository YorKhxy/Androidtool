v1.0.12  (2026-06-04)

- 热更服务器 serve-updates.js 防滥用加固（限流 + 反 slowloris）
- 新增「按当前包名导出完整日志」
- 检查更新加 0.5s 冷却防刷，启动检到新版在按钮旁常驻提示
- 启动中全屏遮罩，初始化完成前禁止操作
- 更新静默安装到当前目录+自动重启，工具内显示安装遮罩
- 标题栏加手动「检查更新」按钮（含已是最新/失败反馈）
- 更新检查失败对用户静默（服务器没开不再弹检查失败提示）
- 更新提示框加大（宽 300->440，更新说明区 120->300、字号加大）
- 热更改为手动触发并修复更新提示框乱码
- 更新日志弹窗关闭按钮乱码（JSX 子文本里的非 ASCII × 未走 JS 字符串）
- 客户端上报更新状态，服务器可判断朋友更新成功/失败
- 热更服务器日志显示客户端 IP 与动作（检查更新/下载更新）
- 应用内查看更新日志（点标题栏版本号弹窗）
- 标题栏显示应用版本号（便于确认是否更新到最新版）
- 打包时从 git 提交自动生成本次更新说明（免手写）
- 热更新展示本次更新说明（release notes）
- 更新服务器启动脚本去掉 echo 里的尖括号（cmd 误判重定向报错）
- 完整日志落盘改到 exe 目录的 device-logs（不再放 C 盘 userData）
- 日志导出按钮 tooltip 乱码（JSX 属性内 \u 不解释），并给「导出」补 tooltip
- 新增导出完整原始日志（全等级、从监控第一行起、不受2万条上限）
- 投屏默认参数改为分辨率上限1280/码率4M
- 投屏声音按实际模式提示两边出声/降级仅电脑
- 投屏声音支持设备与电脑两边同时出声（audio-dup，低版本自动降级）
- NSIS 安装器改为向导式、可选安装目录（不再默认装C盘）
- 热更包按版本归档到 update-releases，服务器服务 latest 目录
- 脚本改 CRLF/纯英文，修复 cmd 把 .bat 注释当命令执行的报错
- 打热更包时自动 patch 自增版本号
- 接入 electron-updater 自动更新（自建 generic 更新服务器）
- 投屏声音支持投屏中实时切换设备/电脑
- 投屏增加声音转发开关，默认声音留在设备
- 文件管理支持批量删除（带二次确认）
- 恢复提示区分上传/下载（方向徽标+配色+箭头）
- 优雅退出不再清空 journal，保留进行中传输为可恢复
- 传输恢复提示改为进入设备文件管理时就地触发
- 启动恢复弹窗改拉取式，修复 did-finish-load 推送早于订阅导致弹窗不出现
